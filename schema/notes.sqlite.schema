CREATE TABLE notes
(
       id INTEGER PRIMARY KEY,
       note_id TEXT,
       author_id TEXT,
       body TEXT,
       created INTEGER,
       lastmodified INTEGER
);

CREATE UNIQUE INDEX `notes_by_author_id` ON notes (`author_id`, `note_id`);
CREATE INDEX `notes_by_created` ON posts (`created`);
       